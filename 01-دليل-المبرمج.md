# دليل المبرمج — تطبيق «أهل الحديث والأثر» لأندرويد

الحزمة: `org.murabbie.ahlalhadeeth` — الإصدار الحالي ١٫٧٫٧ (versionCode ١٨). التطبيق نسخة أندرويد من برنامج ويندوز «أهل الحديث والأثر» (alathar.net، المثبِّت 4.14.0): عشرة مشايخ، ٥٥ كتابًا، ١٣٬٧١٦ شريطًا، ٢٦٢٬٥٨٩ مقطعًا مفهرسًا بالأزمنة، ١١١٬٥٨٠ تفريغًا نصيًا، ١٬١٩٦ تصنيفًا فقهيًا؛ ويزيد عليه محتوى يضيفه المشرفون (سلاسل لعلماء آخرين من يوتيوب أو ملفات) مع فهرسة وتفريغ بالذكاء الاصطناعي.

## ١. البيئة وأدوات البناء

| الأداة | الإصدار المستعمل |
|---|---|
| JDK | ٢١ (يعمل ١٧ أيضًا) |
| Gradle | ٨٫١٤٫٣ (لا يوجد wrapper في المشروع؛ ثبّت Gradle أو أضف wrapper) |
| Android Gradle Plugin | ٨٫٧٫٣ |
| Kotlin | ٢٫٠٫٢١ + إضافة Compose Compiler |
| compileSdk | ٣٦ (أندرويد ١٦) — `android.suppressUnsupportedCompileSdk=36` في `gradle.properties` لأن AGP 8.7 يحذّر منه |
| minSdk / targetSdk | ٢٤ / ٣٥ لنكهة `direct`، و٣٦ لنكهة `play` |
| Compose BOM | 2024.12.01، Material 3 |
| Media3 (ExoPlayer + Session) | ١٫٥٫١ مع `media3-datasource-okhttp` |
| SQLite | `androidx.sqlite:sqlite-bundled:2.5.2` (محرك مدمج يدعم FTS5، ومتوافق مع صفحات ١٦ ك.ب) |
| OkHttp | ٤٫١٢٫٠ |
| NewPipe Extractor | v0.26.5 — في نكهة `direct` فقط |

مكونات SDK اللازمة: `platforms;android-36`، `build-tools;35.0.0` (أو أحدث). في `android/local.properties` ضع `sdk.dir=/مسار/SDK`.

### أوامر البناء

```bash
cd source/android
# نكهة التوزيع المباشر (APK مقسّم حسب المعالج + شامل):
gradle :app:assembleDirectRelease \
  -PmanifestUrl=https://files.murabbie.org/fsdownload/gIX4l2mhu/manifest.json \
  -PsharedUrl=https://files.murabbie.org/fsdownload/4aPZIQpaW/shared-content.json \
  -PadminsUrl=https://files.murabbie.org/fsdownload/jbktInN4f/admins.json
# نكهة Google Play (حزمة AAB):
gradle :app:bundlePlayRelease -PmanifestUrl=… -PsharedUrl=… -PadminsUrl=…
```

المخرجات: `app/build/outputs/apk/direct/release/app-direct-{arm64-v8a,armeabi-v7a,universal}-release.apk` و`app/build/outputs/bundle/playRelease/app-play-release.aab`.

الخصائص `-P` تُضمَّن في `BuildConfig` روابطَ افتراضية (`DEFAULT_MANIFEST_URL`، `DEFAULT_SHARED_URL`، `DEFAULT_ADMINS_URL`)؛ بدونها تُبنى قيم وهمية فلا يجد التطبيق بياناته. هذه الروابط الثلاثة روابط مشاركة **دائمة** على NAS ولا تتغير عند رفع نسخة جديدة من الملف بالاسم نفسه.

التوقيع: `keystore.properties` في `source/android/` (انظر `keystore.properties.example`) يشير إلى `release.keystore` (alias: `ahlalhadeeth`). كلاهما في ملف الأسرار. **كل التحديثات يجب أن توقَّع بالمفتاح نفسه** وإلا رفض أندرويد تثبيتها فوق النسخة القديمة.

`versionCode` و`versionName` في `app/build.gradle.kts`؛ يُرفع الأول بواحد في كل إصدار.

### نكهتا البناء (`productFlavors`, البُعد `distribution`)

| | `direct` | `play` |
|---|---|---|
| الغرض | التوزيع المباشر (APK من NAS) | متجر Google Play |
| استخراج/تنزيل وسائط يوتيوب (نقل الدروس إلى الخادم) | نعم (`src/direct/java/.../YouTubeMedia.kt` بمكتبة NewPipe) | لا — بديل `src/play/java/.../YouTubeMedia.kt` يرمي خطأً مفهومًا، والواجهات تُخفي خيارات النقل (`Chapter.needsTransfer` يعيد false) |
| التحديث الذاتي من `manifest.json` | نعم (بطاقة «يتوفر إصدار جديد» تفتح صفحة التنزيل) | معطَّل (`DataManager.checkAppUpdateDaily` يعود فورًا) — التحديث من المتجر وحده |
| نوع الخدمات الأمامية للنقل والتفريغ | `specialUse` على API 34+ (بلا مهلة) | `dataSync` فقط (مهلة ٦ ساعات يوميًا في أندرويد ١٥+، مع `onTimeout`) — تراكب `src/play/AndroidManifest.xml` يحذف إذن `FOREGROUND_SERVICE_SPECIAL_USE` |
| targetSdk | ٣٥ | ٣٦ |

الفارق يُقرأ في الكود من `BuildConfig.DISTRIBUTION` (`"direct"` أو `"play"`).

### اختبارات JVM

`source/jvmtest/` مشروع Gradle مستقل يترجم ملفات `data/` الخالصة (بلا أندرويد) ويشغّل فحوصات على قاعدة البيانات والبحث والتطبيع وتحليل ردود Gemini وأنواع الوسائط والبث. يحتاج إلى نسخة `ahl_alhadeeth.db` محليًا:

```bash
cd source/jvmtest && gradle run --args="/path/to/ahl_alhadeeth.db db"
```

(قاعدة البيانات تُنزَّل من NAS: أجزاء `ahl_alhadeeth.db.gz.001–007` في `manifest.json`، أو تُبنى من مثبِّت ويندوز بـ `convert.py`.)

## ٢. خريطة المصدر (`source/android/app/src/main/java/org/murabbie/ahlalhadeeth/`)

### `App.kt`
`Application`: يهيّئ `Settings`, `UserDb`, `SharedSync`, `AutoIndexJob`, `TransferJob`, `DataManager`, `AudioDownloads`, `PlayerHolder`، وقنوات الإشعارات، وسجل الأعطال، والتنظيف التلقائي للذاكرة المؤقتة، ومزامنة قائمة خوادم الصوت مع `NasHttp`. وفيه `mediaLoader`: تحميل وسائط درس إلى ملف مؤقت لإرساله إلى Gemini (من الخادم/رابط مباشر/يوتيوب/ملف محلي) مع تقدّم، ثم `MediaPrep.prepare` لتهيئته.

### `data/` — البيانات والمنطق
| الملف | الدور |
|---|---|
| `Db.kt` | فتح قاعدة SQLite المدمجة (`sqlite-bundled`) واستعلامات مساعدة |
| `Models.kt` | النماذج: `Sheekh`, `SheekhBook`, `Chapter` (الشريط/الدرس؛ الرموز السالبة للمحتوى المضاف)، `Segment` (المقطع بالزمن)، `Category` … |
| `Repository.kt` | استعلامات القراءة على قاعدة المحتوى + دمج المحتوى المضاف (`UserContent`) في التصفح والبحث والتصانيف |
| `ArabicText.kt` | التطبيع العربي (للبحث والفهرسة)، الأرقام الهندية، تنسيق الزمن والحجم |
| `DataManager.kt` | جلب `manifest.json`، تنزيل قاعدة البيانات على أجزاء مع استئناف وتحقق sha256 وفك ضغط (`DataDownloadService`)، حالات `DataState`، وفحص تحديث التطبيق |
| `Settings.kt` | الإعدادات (DataStore/Prefs): مصدر الصوت المختار، خوادم الصوت، التنظيف التلقائي، آخر جودة استيراد… |
| `AudioUrls.kt`, `AudioSource.kt` | بناء عنوان ملف الصوت على أي خادم (قاعدة تُلحق بها المسار أو قالب فيه `{path}`)، واختيار المصدر (منزَّل/مجلد محلي/خادم) |
| `NasHttp.kt` | عميل OkHttp المشترك: معترض روابط مشاركة DSM (`fsdownload` و`_sharing_id`) يهيّئ الجلسة ويعيد المحاولة، ومعترض التبديل التلقائي بين خوادم الصوت عند تعذر ملف، ورفع multipart يدوي إلى FileStation (DSM يرفض أجزاء multipart التي تحمل Content-Length) |
| `ParallelDownload.kt` | تنزيل بأربعة اتصالات متوازية على أجزاء ٤ م.ب مع ملف `.chunks` للاستئناف وتجديد الرابط عند 403/410 |
| `AudioDownloads.kt`, `DownloadService.kt` | مدير تنزيل الأشرطة للاستماع بلا إنترنت (خدمة أمامية) |
| `UserDb.kt` | قاعدة المستخدم: المفضلة، سجل الكلمات، التنزيلات، سجل الاستماع؛ وتضم `UserContent` |
| `UserContent.kt` | المحتوى المضاف (مشايخ/سلاسل/دروس/مواضع بمعرّفات سالبة)، حزم JSON (`ahl-alhadeeth-pack`)، الدمج بمفاتيح ثابتة وشواهد حذف، علامة `dirty` لما لم يُنشر |
| `Admins.kt` | نظام المشرفين المستقل: `admins.json` (PBKDF2 للأرقام السرية، AES-GCM لتغليف حساب الخادم ومفتاح Gemini لكل مشرف) |
| `SharedSync.kt` | جلب `shared-content.json` عند الفتح (كل ٦ ساعات) ودمجه، تسجيل دخول المشرف، النشر (دمج آخر نسخة ثم رفعها بواجهة DSM)، إدارة المشرفين ومفاتيح الخادم |
| `YouTube.kt` | معرّفات يوتيوب من الروابط، قراءة قوائم التشغيل كاملة عبر youtubei/browse، جلب التفريغ الآلي (captions) بديلًا |
| `YouTubeMedia.kt` (في `src/direct` و`src/play`) | استخراج روابط وسائط يوتيوب على الهاتف (NewPipe) — انظر النكهات |
| `TransferJob.kt`, `TransferService.kt`, `Mp4Mux.kt` | نقل دروس يوتيوب/الروابط إلى الخادم: تنزيل متوازٍ → (دمج فيديو HD مع الصوت بـ MediaMuxer) → رفع إلى `/downloads/ahl-alhadeeth/media/<مفتاح السلسلة>/` → رابط مشاركة دائم → نشر تلقائي؛ قائمة محفوظة تُستأنف، وخدمة أمامية بإشعار |
| `AutoIndex.kt` | `TopicSegmenter` (استنباط آلي بالقرائن)، `GeminiClient` (REST: generateContent/streamGenerateContent مع تدوير النماذج عند 404/429/503، responseSchema، Files API، خطأ `QuotaError`)، `AutoIndexer.run` (تفريغ الصوت بالذكاء الاصطناعي أصلًا ويوتيوب بديلًا)، `AutoIndexJob` (طابور دفعات محفوظ مع تقدّم مئوي وتوقف عند نفاد الحصة) |
| `AutoIndexService.kt` | خدمة أمامية تُبقي التفريغ حيًّا خارج التطبيق وتعرض نسبته |
| `AacAdts.kt`, `MediaMime.kt` | تهيئة الوسائط لـ Gemini: فحص المسارات، استخراج AAC إلى ADTS بلا إعادة ترميز، تصحيح نوع MIME (WebM الصوتي يُرسل `audio/webm`) |
| `Maintenance.kt` | `CrashLog` (سجل الأعطال في `filesDir/crash.log`) و`CacheCleaner` (تنظيف تلقائي بحد ٢٠٠ م.ب) |

### `player/`
`PlayerHolder` (حالة المشغّل، قائمة التشغيل، المزامنة مع فهرس المقاطع) و`PlaybackService` (MediaSessionService من Media3 مع إشعار الوسائط).

### `ui/`
`MainActivity` (استئناف قوائم النقل والتفريغ، فتح الشاشة من الإشعارات عبر `open`)، `AppRoot.kt` (المسارات `Routes`، الهيكل، شريط التنقل السفلي، أشرطة النقل والتفريغ والمشغّل المصغّر، معالجة لوحة المفاتيح من حافة إلى حافة)، `Components.kt` (مكونات مشتركة)، `theme/`، و`screens/`:

| الشاشة | الملف |
|---|---|
| المشايخ ← الكتب ← الأشرطة (مع وضع تحديد الدروس للمشرف) | `BrowseScreens.kt` |
| الشريط (فهرس المقاطع والقفز بالزمن) والتفريغ الكامل | `TapeScreen.kt`, `TapeTranscriptScreen.kt`, `TranscriptScreen.kt` |
| البحث (FTS5 مع تظليل) | `SearchScreen.kt` |
| التصانيف الفقهية | `CategoryScreens.kt` |
| المفضلة، التنزيلات، المزيد (الإحصائيات/السجل/حول) | `FavoritesScreen.kt`, `DownloadsScreen.kt`, `MoreScreens.kt` |
| المشغّل الكامل | `PlayerScreen.kt` |
| الإعداد الأول (تنزيل البيانات تلقائيًا) | `SetupScreen.kt` |
| الإعدادات (مصدر الصوت والخوادم، الصيانة: الذاكرة المؤقتة وسجل الأعطال) | `SettingsScreen.kt` |
| المحتوى المضاف: مشايخ/سلاسل/دروس/محرر المواضع/الاستيراد والتصدير | `UserContentScreens.kt` |
| استيراد يوتيوب ومشغّل IFrame المضمَّن | `YouTubeScreens.kt` |
| الفهرسة والتفريغ التلقائيان (درس واحد / دفعة) | `AutoIndexScreens.kt` |
| نقل الدروس إلى الخادم | `TransferScreen.kt` |
| المشرفون ومفاتيح الخادم | `AdminsScreen.kt` |

## ٣. البيانات على NAS (سينولوجي، DSM)

الواجهة: `https://files.murabbie.org` (منفذ ٤٤٣). المجلد المشترك العام `downloads`، ومجلد التطبيق `/downloads/ahl-alhadeeth`:

| المسار/الملف | الوصف |
|---|---|
| `manifest.json` | بيان التطبيق والبيانات (رابطه الدائم `fsdownload/gIX4l2mhu/manifest.json`) — يولّده `make_manifest.py` |
| `ahl_alhadeeth.db.gz.001–007` + `SHA256SUMS.txt` | قاعدة البيانات مضغوطة على أجزاء (١٧٧ م.ب، وبعد الفك ٥٧٠ م.ب) |
| `shared-content.json` | المحتوى المشترك الذي ينشره المشرفون (رابطه الدائم `fsdownload/4aPZIQpaW/…`) |
| `admins.json` | ملف المشرفين (رابطه الدائم `fsdownload/jbktInN4f/…`) — عام لكنه لا يحوي إلا بصمات ومفاتيح مغلَّفة |
| `sound/<الشيخ>/<الكتاب>/NNN.mp3` | النسخة الكاملة من صوت الأثر (١٣٬٧١٥ ملفًا، ١٧٦ ج.ب) — نُسخت بـ `mirror_to_nas.py`؛ الحالة في `sound/_mirror_state.json` |
| `media/<مفتاح السلسلة>/` | وسائط الدروس المنقولة من يوتيوب (كل ملف برابط مشاركة خاص) |
| `ahl-alhadeeth-<الإصدار>-{arm64,arm32,universal}.apk` | ملفات التوزيع المباشر |
| `README.md`, `ahl-alhadeeth-android-project.tar.gz` | الدليل والمصدر (بلا مفتاح التوقيع) |

**رابط مشاركة المجلد الدائم `ehniFEwJg`** يُستعمل قالبًا لخادم الصوت على NAS (`audio.servers_v2` في manifest):
`…/webapi/entry.cgi?api=SYNO.FolderSharing.Download&version=2&method=download&mode=open&_sharing_id=ehniFEwJg&path=%5B%22%2Fdownloads%2Fahl-alhadeeth%2Fsound%2F{path}%22%5D`
لا تُلغِ هذه المشاركة أبدًا. تحتاج إلى كعكة `sharing_sid` (تُؤخذ من `/sharing/ehniFEwJg`) ويهيّئها `NasHttp` تلقائيًا؛ تدعم `Range`؛ أخطاؤها تأتي بحالة 200 وجسم JSON (يعالجه `looksBad`).

**قواعد NAS:** أي شيء في `downloads` يمكن لأي أحد يملك رابط مشاركة تنزيله، فلا سرّي فيه. حساب `manus` (الذي يستعمله التطبيق للنشر وتستعمله السكربتات) بلا دخول shell وصلاحيته قراءة/كتابة على `/downloads` فقط.

### `manifest.json` (مختصر)
```json
{ "version": 1, "name": "أهل الحديث والأثر", "updated": "2026-09-14",
  "app": { "version_code": 18, "version_name": "1.7.7", "apk": {"arm64": "…", "arm32": "…", "universal": "…"}, "apk_page": {…}, "notes": "…" },
  "data": { "id": "ahl_alhadeeth", "version": "4.14.0-1", "file": "ahl_alhadeeth.db.gz", "size_gz": …, "size_db": …, "sha256_gz": "…", "sha256_db": "…",
            "parts": [{"url": "…/fsdownload/…/ahl_alhadeeth.db.gz.001", "alt_url": "…", "size": …}, …] },
  "packs": [ … حزم اختيارية … ],
  "audio": { "ext": ".mp3", "servers": [{"title": "…", "url": "https://www.alathar.net/files/sound/"}],
             "servers_v2": [{"title": "خادم البيانات (NAS)", "url": "<القالب أعلاه>"}], "default_server": "<القالب>" } }
```

### `shared-content.json` (حزمة `ahl-alhadeeth-pack`)
`{ format, version, name, exported_at, id, updated, published_by, pack_version, removed: [مفاتيح محذوفة], sheekhs: [ { name, key, series: [ { name, key, tapes: [ { title, media_url, kind: "audio"|"video", source_url, size, key, segments: [ { start: "h:mm:ss", start_ms, line, write?, ques?, hnum?, categories? } ] } ] } ] } ] }`
كل عنصر له `key` ثابت (يُولَّد من المصدر) حتى يُدمج عند كل المستخدمين بلا تكرار؛ عند الاستيراد تُستبدل مواضع الدرس المطابق ما لم يكن معدَّلًا محليًا وغير منشور.

### `admins.json`
`{ format, version, updated, kdf: {alg: "PBKDF2-HMAC-SHA256", iterations: 100000}, super: {user, name, salt, hash, wrapped, updated}, admins: [ {user, name, salt, hash, wrapped, active, updated} ] }`
`hash` = بصمة الرقم السري؛ `wrapped` = `user\npass[\ngeminiKey]` مغلَّفة AES-256-GCM بمفتاح مشتق من الرقم السري. المشرف العام اسمه `admin`؛ رقمه السري الحالي يعرفه المالك وحده (غيّره في ١٣ سبتمبر ٢٠٢٦). أداة الطوارئ: `admins_tool.py` (إنشاء/إضافة/فحص).

## ٤. السكربتات (Python 3، تعمل من أي حاسوب)

| السكربت | الوظيفة |
|---|---|
| `nas.py` | مكتبة صغيرة لواجهة DSM FileStation (تسجيل دخول، رفع، مشاركة، حذف، سرد). كلمة سر `manus` تُقرأ من متغير البيئة `NAS_PASSWORD` في نسخة الحزمة |
| `make_manifest.py` | توليد `manifest.json` من `SHA256SUMS.txt` وروابط الأجزاء (`shares_parts.jsonl`) وروابط APK (`shares_apk.json`)؛ المتغيرات `APP_VERSION_CODE`, `APP_VERSION_NAME`, `APP_NOTES`, `AUDIO_DEFAULT_SERVER` |
| `convert.py`, `sfx.py` | تحويل قواعد Access في مثبِّت ويندوز (`setup-4-14-0.exe`, Setup Factory) إلى SQLite مع FTS5 |
| `mirror_to_nas.py` | نسخ صوت الأثر كله إلى NAS (قابل للاستئناف؛ `--workers`) |
| `yt2nas.py` | نقل قائمة يوتيوب إلى NAS من حاسوب (yt-dlp + ffmpeg) وتحديث `shared-content.json` ونشره — لا يعمل من مراكز البيانات (يوتيوب يطلب تسجيل الدخول منها) |
| `transcribe_series.py` | تفريغ سلسلة كاملة من الخادم بـ Gemini (تقسيم بـ ffmpeg إلى أجزاء ≈٣٠ دقيقة mp3، Files API، مخرجات منظَّمة، دمج ونشر؛ حالة في `transcribe_state.json` وحجب النماذج في `gemini_blocked.json`؛ `--max-seconds` للدفعات، `--replace` لإعادة الفهرسة) |
| `admins_tool.py` | إدارة `admins.json` (يحتاج مكتبة `cryptography`) |

مهمة مجدولة قائمة (في جلسة Claude التي طوّرت المشروع): تشغيل `transcribe_series.py --all` يوميًا الساعة ٠٧:٤٠ UTC حتى تكتمل سلسلة «شرح بلوغ المرام» (١١٠ دروس، فُرّغ منها ٢٢ حتى ١٤ سبتمبر). يمكن للمبرمج تشغيل الأداة من أي خادم بدلًا منها.

## ٥. Gemini (التفريغ بالذكاء الاصطناعي)

- المفتاح يضبطه المشرف العام داخل التطبيق («المشرفون ← مفاتيح الخادم والذكاء الاصطناعي») فيُغلَّف في `admins.json` لكل مشرف. لا يوجد خادم وسيط: التطبيق يتصل بـ `generativelanguage.googleapis.com` مباشرة.
- النماذج (سبتمبر ٢٠٢٦): `gemini-3.5-flash` ← `gemini-3.6-flash` ← `gemini-3-flash-preview` ← `3.8` ← `3.7` ← `flash-latest`؛ القائمة في `GeminiClient.MODELS` (نماذج 2.5 لم تعد متاحة للمفاتيح الجديدة). التدوير تلقائي عند 404/429/503.
- الطبقة المجانية: ٢٠ طلبًا يوميًا لكل نموذج لكل مشروع (تتجدد نحو ٠٧:٠٠ UTC). الدرس الطويل = طلب لكل نافذة ٣٠ دقيقة. لسلاسل كبيرة فعّل الفوترة لمشروع المفتاح.
- التقدّم المئوي حقيقي: البث SSE يُقرأ وتُستخرج أزمنة `"start"` كلما وصلت (`GeminiClient.streamedFraction`).

## ٦. ما يستحق التحسين لاحقًا (اقتراحات)

1. **اختبار نكهة `play` على أجهزة أندرويد ١٦ حقيقية** (targetSdk 36): الرجوع التنبؤي، من حافة إلى حافة، إشعارات الخدمات، Media3.
2. **الملفات الكبيرة:** فتح قاعدة ٥٧٠ م.ب يحتاج نحو ٨٠٠ م.ب مساحة مؤقتة عند أول تجهيز؛ يمكن تقسيم قاعدة التفريغات عن الفهارس لتصغير التنزيل الأول.
3. **تنزيل يوتيوب** يعتمد على مكتبة NewPipe التي تتعطل مع تغييرات يوتيوب؛ حدّث نسختها عند اللزوم (نكهة `direct` فقط).
4. **مفاتيح Gemini** متاحة لكل مشرف يفك التغليف؛ إن كثر المشرفون فالأنسب خادم وسيط صغير يحمل المفتاح.
5. **اختبارات أندرويد** (Instrumented) غير موجودة؛ الاختبارات الحالية JVM فقط.
6. تحويل المشروع إلى Gradle wrapper وGitHub/GitLab مع CI للبناء والتوقيع.

## ٧. سجل الإصدارات

انظر `README-دليل-المستخدم-والمشرف.md` (كل إصدار وما فيه من ١٫٠٫٠ إلى ١٫٧٫٧).
