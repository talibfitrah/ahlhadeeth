# النشر على Google Play — خطوة خطوة

هذا الدليل مبني على متطلبات Google Play كما هي في سبتمبر ٢٠٢٦ (يُستحسن مراجعة مركز سياسات Play قبل الرفع، فهي تتغير). الملف الجاهز للرفع: `builds/ahl-alhadeeth-1.7.7-play-release.aab` (نكهة `play`، موقَّع بمفتاح الإصدار، targetSdk 36، versionCode 18) — **لا يُرفع هذا الملف:** بعد مراجعة ٢٠ سبتمبر ٢٠٢٦ صار الإصدار ١٫٧٫٨ (versionCode 19) ويجب بناؤه من المصدر الحالي. لكل تحديث لاحق يُبنى ملف AAB جديد بـ `versionCode` أكبر.

## ٠. قبل كل شيء: أمور تخص هذا التطبيق تحديدًا

1. **تنزيل وسائط يوتيوب مخالف لسياسات المتجر وشروط يوتيوب.** لذلك نكهة `play` **لا** تحوي مكتبة NewPipe ولا خيار «نقل الدروس إلى الخادم». لا تُعِد هذه الوظيفة إلى نسخة المتجر أبدًا؛ نقل الدروس يُجرى من نسخة التوزيع المباشر أو بأداة `yt2nas.py` من حاسوب. مشغّل يوتيوب المضمَّن (IFrame API الرسمي) مسموح.
2. **التحديث الذاتي ممنوع** («سياسة إساءة استخدام الجهاز والشبكة»: لا يجوز للتطبيق أن يحدّث نفسه بغير آلية المتجر). نكهة `play` تعطّل فحص `manifest.json` للتحديث.
3. **حقوق المحتوى:** الدروس الصوتية من موقع alathar.net ومن قنوات يوتيوب. احتفظ بما يثبت الإذن بإعادة النشر (أو أن المحتوى منشور للتوزيع الحر بإذن أصحابه) لأن المتجر قد يطلبه بموجب سياسة «الملكية الفكرية»، واذكر المصدر في وصف التطبيق.
4. **الخدمات الأمامية:** نسخة المتجر تستعمل `mediaPlayback` (المشغّل) و`dataSync` (تنزيل البيانات والأشرطة، النقل، التفريغ). كلاهما يحتاج إلى «تصريح الخدمات الأمامية» في Play Console (نص مقترح في الملف `04`). نوع `specialUse` أُزيل من نسخة المتجر لأنه يحتاج إلى تبرير خاص وقد يُرفض.
5. **التنزيل الأول كبير** (١٧٧ م.ب مضغوطة، ٥٧٠ م.ب بعد الفك): مسموح، لكن اذكره في الوصف وفي لقطات الشاشة حتى لا تأتي تقييمات سلبية.
6. **الحد الأدنى للأجهزة:** أندرويد ٧ (API 24)، معالجات arm64 وarm32 (AAB يولّد النسخ تلقائيًا).
7. **صفحات ١٦ ك.ب:** المكتبات الأصلية في التطبيق (`libsqliteJni.so`, `libandroidx.graphics.path.so`) متوافقة (محاذاة `0x4000`) — تحقق منها بعد أي ترقية للمكتبات: `zipalign -c -P 16 -v 4 app.apk`.

## ١. حساب المطوّر

- سجّل في [Play Console](https://play.google.com/console) (رسم ٢٥ دولارًا مرة واحدة). حساب **مؤسسة** (باسم Stichting Tarbiyah مثلًا) يحتاج إلى رقم D‑U‑N‑S وتحقق من المؤسسة، لكنه يعفيك من شرط الاختبار المغلق أدناه ويُظهر اسم المؤسسة في المتجر. الحساب **الشخصي** أسرع تسجيلًا لكنه يستوجب:
- **شرط الحسابات الشخصية الجديدة (بعد ١٣ نوفمبر ٢٠٢٣):** قبل السماح بالنشر العام يجب إجراء اختبار مغلق بـ **١٢ مختبِرًا على الأقل منضمّين باستمرار ١٤ يومًا**، ثم «طلب الوصول إلى الإنتاج» من لوحة التحكم (يُراجَع في نحو أسبوع). جهّز ١٢–١٥ بريدًا إلكترونيًا (Gmail) لمختبرين حقيقيين (طلبة، أهل، إخوة) وأبلغهم ألّا يخرجوا من البرنامج خلال المدة.

## ٢. إنشاء التطبيق في Play Console

1. **Create app**: الاسم «أهل الحديث والأثر»، اللغة الافتراضية العربية، «App» (لا لعبة)، مجاني، ثم أقرّ بالتصريحات.
2. **Play App Signing** (يظهر عند أول رفع): **اختر «Use an existing key»** (تصدير المفتاح من `release.keystore` بأداة PEPK ورفعه) حتى تكون توقيعات نسخة المتجر مطابقة لتوقيع ملفات APK الموزَّعة مباشرة، فيستطيع من ثبّت التطبيق من NAS التحديث من المتجر والعكس. الأمر (بعد تنزيل `pepk.jar` من صفحة الإعداد في Console):
   ```bash
   java -jar pepk.jar --keystore=release.keystore --alias=ahlalhadeeth \
     --output=upload_key.zip --include-cert --rsa-aes-encryption \
     --encryption-key-path=/path/to/encryption_public_key.pem
   ```
   ثم ارفع `upload_key.zip`. (إن تركت Google تولّد مفتاح التوقيع فسيصبح مفتاحك «مفتاح رفع» فقط، وحينها لا يمكن تثبيت نسخة المتجر فوق نسخة NAS ولا العكس إلا بحذف التطبيق — تجنّب ذلك.)
3. **Set up your app** (قسم App content): أكمل كل البنود — النصوص المقترحة لكل بند في الملف `04`:
   - سياسة الخصوصية: رابط صفحة عامة تحوي نص الملف `03` (يمكن نشره على GitHub Pages أو على موقع المؤسسة؛ رابط مشاركة NAS يصلح مؤقتًا لكن الأفضل صفحة HTML ثابتة).
   - الإعلانات: لا يحوي إعلانات.
   - App access: الوظائف العامة كلها متاحة بلا تسجيل؛ وظائف المشرفين (إضافة محتوى) مقيّدة برقم سري. أنشئ من داخل التطبيق (بحساب المشرف العام) مشرفًا باسم `reviewer` برقم سري مخصص للمراجعة، واذكر في النموذج كيف يُدخل («المزيد ← المحتوى المضاف ← دخول المشرف»)، أو اختر «كل الوظائف متاحة» إن اعتبرت إدارة المحتوى ليست من وظائف المستخدم.
   - تصنيف المحتوى (استبيان IARC): تطبيق تعليمي/مرجعي، لا عنف ولا محتوى للبالغين ← غالبًا «Everyone / PEGI 3».
   - الجمهور المستهدف: ١٨ فما فوق (أو ١٣ فما فوق). لا تختر فئات دون ١٣ حتى لا تنطبق سياسات «العائلات».
   - أمان البيانات (Data safety): التطبيق لا يجمع ولا يشارك بيانات شخصية (لا حسابات، لا تحليلات، لا معرّف إعلانات)؛ الاتصال مشفَّر (HTTPS).
   - تصريح الخدمات الأمامية (Foreground service permissions): `mediaPlayback` و`dataSync` — انظر النص في `04`. قد يُطلب فيديو قصير يُظهر الوظيفة (إشعار المشغّل، وإشعار تنزيل البيانات).
   - Government / financial / health / news: لا.
4. **Store listing**: العنوان، الوصف القصير (٨٠ حرفًا)، الوصف الكامل (حتى ٤٠٠٠) من الملف `04`، الأيقونة `store-assets/icon-512.png`، الرسم المميز `store-assets/feature-graphic-1024x500.png`، **لقطات شاشة للهاتف (٢ على الأقل، يُفضَّل ٦–٨)** تُلتقط من جهاز حقيقي: الشاشة الرئيسية، الأشرطة، فهرس الشريط مع المشغّل، التفريغ، البحث، التصانيف. الفئة: Education (أو Books & Reference). بريد التواصل مطلوب.

## ٣. الرفع والاختبار ثم الإنتاج

1. **Testing → Internal testing → Create new release**: ارفع `ahl-alhadeeth-1.7.7-play-release.aab`، اكتب ملاحظات الإصدار (بالعربية والإنجليزية)، احفظ وراجع ثم Start rollout. أضف بريدك وبريد المالك في قائمة المختبرين وثبّت من رابط الاختبار على أجهزة **أندرويد ١٦** و**أندرويد ٧–٩** (أقدم الأجهزة المدعومة) وتحقق من: التنزيل الأول للبيانات واستئنافه بعد إغلاق التطبيق، التشغيل من الخادم مع الإشعار، البحث، التصانيف، المفضلة، التنزيل للاستماع بلا إنترنت، ودخول المشرف وإضافة سلسلة من يوتيوب (بلا نقل) وتفريغها (يلزم مفتاح Gemini).
2. **Closed testing** (إلزامي للحساب الشخصي الجديد): أنشئ مسارًا مغلقًا، ارفع الإصدار نفسه، أضف ١٢+ مختبِرًا، وانتظر ١٤ يومًا متصلة، ثم اطلب الوصول إلى الإنتاج.
3. **Production**: ارفع الإصدار (أو رقّ الإصدار من مسار الاختبار)، اختر الدول (كل الدول)، وأرسل للمراجعة. أول مراجعة قد تستغرق من أيام إلى أسبوعين.
4. بعد النشر: راقب «Android vitals» (الأعطال) و«Policy status». الأعطال تظهر أيضًا داخل التطبيق في «الإعدادات ← الصيانة ← سجل الأعطال».

## ٤. إصدار تحديث

```bash
# ١. ارفع versionCode (١٩، ٢٠، …) وversionName في app/build.gradle.kts
# ٢. ابنِ الحزمة:
gradle :app:bundlePlayRelease -PmanifestUrl=… -PsharedUrl=… -PadminsUrl=… -PprivacyUrl=…
# ٣. Play Console → Production → Create new release → ارفع ملف AAB الجديد
```
وللتوزيع المباشر بالتوازي: `gradle :app:assembleDirectRelease` ثم رفع ملفات APK وتحديث `manifest.json` (بـ `make_manifest.py`) كما في دليل المستخدم — استعمل `versionCode` واحدًا للنكهتين في الإصدار الواحد.

## ٥. قائمة تحقق قبل كل رفع

- [ ] `versionCode` أكبر من السابق، والتوقيع بـ `release.keystore` نفسه.
- [ ] البناء من نكهة `play` (`bundlePlayRelease`) لا `direct`.
- [ ] لا مكتبة NewPipe في الحزمة (`unzip -p app.apk 'classes*.dex' | grep -a -c schabi` ← ٠؛ والأمر نفسه على APK نكهة direct يجب أن يعطي رقمًا أكبر من صفر، وإلا فالفحص نفسه معطَّل).
- [ ] رابط سياسة الخصوصية مضمَّن (`-PprivacyUrl`): `unzip -p app.apk 'classes*.dex' | grep -a -c '<نطاق الرابط>'` ← ١ فأكثر، ويظهر في «عن البرنامج».
- [ ] الخدمات الأمامية `dataSync`/`mediaPlayback` فقط (لا `specialUse`).
- [ ] targetSdk = آخر مستوى يشترطه المتجر (٣٦ حتى أغسطس ٢٠٢٧ على الأرجح).
- [ ] اختبار على أندرويد ١٦ وعلى جهاز قديم.
- [ ] ملاحظات الإصدار مكتوبة، ورابط سياسة الخصوصية يعمل.
- [ ] `SHA256SUMS`/manifest للتوزيع المباشر محدَّثة إن نُشرت النكهة الأخرى أيضًا.

## المصادر
- متطلبات مستوى API المستهدف: https://support.google.com/googleplay/android-developer/answer/11926878
- متطلبات اختبار الحسابات الشخصية الجديدة: https://support.google.com/googleplay/android-developer/answer/14151465
- سياسة إساءة استخدام الجهاز والشبكة (التحديث الذاتي): https://support.google.com/googleplay/android-developer/answer/16559646
- توافق صفحات ١٦ ك.ب: https://developer.android.com/guide/practices/page-sizes
- سياسات مطوّري YouTube API: https://developers.google.com/youtube/terms/developer-policies
