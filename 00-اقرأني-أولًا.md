# حزمة تسليم مشروع «أهل الحديث والأثر» (أندرويد)

هذه الحزمة تحوي كل ما يحتاجه مبرمج لإكمال تطوير التطبيق ونشره على Google Play. تاريخ التسليم: ١٤ سبتمبر ٢٠٢٦ — آخر إصدار ١٫٧٫٧ (versionCode ١٨).

## محتويات الحزمة

| المسار | المحتوى |
|---|---|
| `01-دليل-المبرمج.md` | بنية المشروع، خريطة الملفات، صيغ البيانات، خادم NAS، أوامر البناء، الأدوات المساعدة، ما يُحسَّن مستقبلًا |
| `02-النشر-على-Google-Play.md` | خطوات النشر على المتجر خطوةً خطوة، والسياسات التي تمس هذا التطبيق تحديدًا، وقائمة تحقق |
| `03-سياسة-الخصوصية.md` | نص سياسة الخصوصية (عربي + إنجليزي) الجاهز للنشر على صفحة عامة — يطلبه المتجر |
| `04-نصوص-المتجر-وإجابات-النماذج.md` | اسم التطبيق ووصفه للمتجر، الفئة، إجابات نموذج «أمان البيانات» و«تصنيف المحتوى» و«تصريح الخدمات الأمامية» |
| `README-دليل-المستخدم-والمشرف.md` | دليل الاستعمال الكامل (كل الإصدارات من ١٫٠ إلى ١٫٧٫٧) كما هو منشور للمستخدمين على NAS |
| `source/` | المشروع المصدري كاملًا: `android/` (Kotlin/Compose)، `jvmtest/` (اختبارات JVM)، وسكربتات Python |
| `builds/ahl-alhadeeth-1.7.7-play-release.aab` | حزمة Google Play (نكهة `play`، موقَّعة بمفتاح الإصدار، تستهدف API 36) — جاهزة للرفع إلى مسار الاختبار الداخلي |
| `builds/ahl-alhadeeth-1.7.7-*.apk` | ملفات التوزيع المباشر (نكهة `direct`) المنشورة حاليًا على NAS |
| `store-assets/` | أيقونة المتجر ٥١٢×٥١٢ والرسم المميز ١٠٢٤×٥٠٠ |
| `data-samples/` | نسخ مرجعية من `manifest.json` و`admins.json` و`pack-example.json` كما هي على NAS |

**ملف الأسرار** (`ahl-alhadeeth-secrets.zip`) يُسلَّم على حدة وفيه مفتاح التوقيع وكلمات السر ومفتاح Gemini. لا تضعه في مستودع عام ولا ترسله إلا لمن تثق به؛ من فقد مفتاح التوقيع لم يستطع تحديث التطبيق عند من ثبّتوه.

## بإيجاز شديد

- التطبيق: Kotlin + Jetpack Compose (Material 3)، Media3 للتشغيل، SQLite مدمج (FTS5) لقاعدة بيانات برنامج ويندوز «أهل الحديث والأثر» (alathar.net) المحوَّلة، والبيانات كلها على NAS سينولوجي عبر روابط مشاركة DSM.
- نكهتا بناء: `direct` (التوزيع المباشر من NAS، كامل الوظائف) و`play` (نسخة المتجر: بلا تنزيل وسائط يوتيوب، بلا تحديث ذاتي، خدمات أمامية من نوع dataSync، تستهدف API 36).
- أول شيء يفعله المبرمج: يفك `source/`، يضع `release.keystore` و`keystore.properties` من ملف الأسرار في `source/android/`، ثم يبني (`02` فيه الأوامر) ويثبّت على جهاز أندرويد ١٦ ويجرّب.
